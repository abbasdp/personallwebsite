{{#checkIfAny @config.custom.featured @config.custom.authors @config.custom.tags @config.custom.newsletter @config.custom.socialButtons}}
   <aside class="right-bar" aria-label="نوار کناری">
      <div class="right-bar__inner">
         <div class="sidebar">
            <section class="box box--mission">
               <h3 class="box__title">مأموریت</h3>
               <p>هر راهی برای ترویج فرهنگ و فلسفهٔ نرم‌افزار آزاد لازم باشد، از آن استفاده می‌کنم و تجربه‌هایم را به اشتراک می‌گذارم.</p>
               <p><a href="https://www.gnu.org/philosophy/free-sw.fa.html" rel="noopener" target="_blank">تعریف نرم‌افزار آزاد (GNU)</a></p>
               <p><a href="https://sudoshz.ir" rel="noopener" target="_blank">جامعهٔ شیرازلینوکس</a></p>
            </section>

            {{#if @config.custom.featured}}
               {{#if featuredPosts}}
                  <section class="box featured">
                     <h3 class="box__title">{{ translate 'partials.sidebar.featured' }}</h3>
                     <ul class="featured__container">
                        {{#each featuredPosts}}
                           {{#checkIf @index '<' @config.custom.featuredAmount}}
                              <li class="featured__item">
                                 {{#if @config.custom.featuredImage}}
                                    {{#featuredImage}}
                                       {{#if url}}
                                          <a href="{{../url}}" class="featured__image" tabindex="-1" aria-hidden="true">
                                             <img
                                                {{#if @config.site.responsiveImages}}
                                                   src="{{urlXs}}"
                                                {{else}}
                                                   src="{{url}}"
                                                {{/if}}
                                                {{ lazyload "lazy" }}
                                                alt="{{#if alt}}{{alt}}{{else}}{{../title}}{{/if}}"
                                                height="{{height}}"
                                                width="{{width}}">
                                          </a>
                                       {{/if}}
                                    {{/featuredImage}}
                                 {{/if}}
                                 <div>
                                    <a href="{{url}}" class="featured__title">{{title}}</a>
                                    {{#if @config.custom.featuredDate}}
                                       <time class="featured__meta" datetime="{{date createdAt 'YYYY-MM-DDTHH:mm'}}">
                                          {{#checkIf @config.custom.formatDate '!=' 'custom'}}
                                             {{date createdAt @config.custom.formatDate}}
                                          {{else}}
                                             {{date createdAt @config.custom.formatDateCustom}}
                                          {{/checkIf}}
                                       </time>
                                    {{/if}}
                                 </div>
                              </li>
                           {{/checkIf}}
                        {{/each}}
                     </ul>
                  </section>
               {{/if}}
            {{/if}}

            {{#if @config.custom.authors}}
               <section class="box authors">
                  <h3 class="box__title">{{ translate 'partials.sidebar.authors' }}</h3>
                  <ul class="authors__cotainer">
                     {{#each authors}}
                        {{#checkIf @index '<' @config.custom.authorsAmount}}
                           <li class="authors__item">
                              {{#if @config.custom.authorsAvatar}}
                                 {{#if avatar}}
                                    <a href="{{url}}" class="authors__image">
                                       <img
                                          src="{{avatarImage.url}}"
                                          {{ lazyload "lazy" }}
                                          alt="{{avatarImage.alt}}"
                                          height="{{avatarImage.height}}"
                                          width="{{avatarImage.width}}"
                                          class="authors__img">
                                    </a>
                                 {{/if}}
                              {{/if}}
                              <div>
                                 <a href="{{url}}" class="authors__title">{{name}}</a>
                                 {{#if @config.custom.authorsPostCount}}
                                    <span class="authors__meta">{{ translate 'partials.sidebar.authorsPost' }} {{postsNumber}}</span>
                                 {{/if}}
                              </div>
                           </li>
                        {{/checkIf}}
                     {{/each}}
                  </ul>
               </section>
            {{/if}}

            {{#if @config.custom.tags}}
               {{#if tags}}
                  <section class="box tags">
                     <h3 class="box__title">{{ translate 'partials.sidebar.tags' }}</h3>
                     <ul class="tags__list">
                        {{#each tags}}
                           {{#checkIf @index '<' @config.custom.tagsAmount}}
                              <li class="tags__item">
                                 <a href="{{url}}" class="btn btn--gray" rel="tag">{{name}}
                                    {{#if @config.custom.tagsPostCount}}
                                       <sup>({{postsNumber}})</sup>
                                    {{/if}}
                                 </a>
                              </li>
                           {{/checkIf}}
                        {{/each}}
                     </ul>
                  </section>
               {{/if}}
            {{/if}}

            {{#if @config.custom.newsletter}}
               <section class="box newsletter">
                  <h3 class="box__title">{{ translate 'partials.sidebar.newsletter' }}</h3>
                  <p class="newsletter__desc">{{ translate 'partials.sidebar.newsletterDesc' }}</p>
                  {{{@config.custom.newsletterFormCode}}}
               </section>
            {{/if}}

            {{#if @config.custom.socialButtons}}
               <section class="box box--social">
                  <h3 class="box__title">شبکه‌های آزاد</h3>
                  {{> social class="follow--list"}}
               </section>
            {{/if}}

         </div>
      </div>
   </aside>
{{/checkIfAny}}
